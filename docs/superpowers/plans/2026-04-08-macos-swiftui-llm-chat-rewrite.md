# SwiftUI macOS LLM App Rewrite Implementation Plan

Implemented from the approved plan. The repository now targets a single macOS SwiftUI app and one test target, with `LocalMLXChatCore` as the only inference dependency.
